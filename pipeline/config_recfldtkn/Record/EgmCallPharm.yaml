# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%% TableBase sources from different cohorts.
CohortInfo: # Cohort
  RawData2023Nov_Trulicity:
    EgmBase: 
      raw_data_path: $DATA_RAW$/2023_11_30_30days_Trulicity/Egm_Call Pharmacy_df_de_identified.csv
      RawRootID: 'patient_id_encoded'
      RecNumColumn: 'Egm_Call Pharmacy' # in Human2RecNum, the related raw table name
      raw_columns: 
        - patient_id_encoded
        - invitation_id_encoded
        - prescription_id_encoded
        - event_id
        - invitation_date
        - event_date
        - p_created_date
        - created_date
        - external_id
        - campaign_id
        - event_name
        - key1
        - value1
        - key2
        - value2
        - key3
        - value3
        - key4
        - value4
        - key5
        - value5
        - server_id
        - partner_id
        - content_id
        - basket_id
        - workflow_step
        - user_agent
        - record_locator_encoded


# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%  where to save the Record data.
RecName: EgmCallPharm    # name of the record.
RecID: EgmCallPharmID   # ID of the record. not necessary to be like this: RecID = RecName + 'ID'.
RecIDChain: 
  - PID
  - PInvID
  - EgmCallPharmID
  # - event_id # or we just ignore this. 

# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Add Parent Rec is Parent Rec is not Patient.
ParentRecName: 'PInv'
RecDT: DT

# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%  the final fld_cols to save.
attr_cols:
  - PID
  - PInvID
  - EgmCallPharmID
  - event_id
  - DT
  - invitation_date
  - event_date
  - server_id
  - user_agent

# the transform py function is available in rawrec2recfld folder.
pypath: 

FldTknInfo:


